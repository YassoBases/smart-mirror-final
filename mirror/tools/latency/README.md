# Deferred partial timing tooling

The timing collector and commit observer are not connected to the application.
`report.py` requires NumPy >=1.24. No latency acceptance result exists. See the
report-alignment handoff under docs/testing/evidence/report-alignment.
