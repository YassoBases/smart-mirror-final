# Deferred partial packet inventory

`audit.py capture.pcap --source-ip <mirror-IP>` inventories destination packet
bytes. It requires PyShark plus TShark, or Scapy. Neither encrypted contents nor
the absence of video can be established from this inventory. This unfinished
tooling is preserved by request; no network audit has been performed.
